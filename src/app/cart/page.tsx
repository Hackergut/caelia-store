import { CartView } from "@/components/cart-view";

export const metadata = {
  title: "Carrello | CAELIA",
  description: "Il tuo carrello CAELIA. Procedi al checkout sicuro Shopify.",
};

export default function CartPage() {
  return <CartView />;
}
