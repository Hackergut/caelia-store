<section class="c-page">
  <div class="shell c-page__box">
    <p class="eyebrow">{{ page.handle }}</p>
    <h1 class="type-display-md">{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Pagina" }
{% endschema %}
