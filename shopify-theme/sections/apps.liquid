<section class="c-row">
  <div class="shell">
    {% for block in section.blocks %}
      {% render block %}
    {% endfor %}
  </div>
</section>
{% schema %}
{
  "name": "t:sections.apps.name_0",
  "settings": [],
  "blocks": [
    {
      "type": "@app"
    }
  ],
  "presets": [
    {
      "name": "t:sections.apps.presets__0.name"
    }
  ]
}
{% endschema %}
