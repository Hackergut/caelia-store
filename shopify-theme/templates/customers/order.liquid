<section class="c-page">
  <div class="shell c-page__box">
    <h1 class="type-display-md">{{ order.name }}</h1>
    <p>{{ order.created_at | date: '%d %B %Y' }} · {{ order.financial_status }}</p>
  </div>
</section>
